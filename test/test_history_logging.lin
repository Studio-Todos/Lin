// RUN: %linc build %s -o %t && %t | FileCheck %s

import "std/types.lin"
import "std/io.lin"
import "std/math.lin"

main: func [
    argc [i32!]
    return: [i32!]
][
    // Verify that the compiler handles function definitions and basic
    // execution correctly under the PICR history logging infrastructure.
    0
]

(print_i32 0 (main 0))
// CHECK: 0
