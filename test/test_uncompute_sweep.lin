// RUN: %linc build %s -o %t && %t | FileCheck %s

import "std/types.lin"
import "std/math.lin"

test_uncompute: func [
    return: [i32!]
][
    // Verify that compilation and execution of standard operations (which lower to combinator nets)
    // runs correctly under the uncompute/reversal execution paths.
    0
]

(print (test_uncompute))
// CHECK: 0
